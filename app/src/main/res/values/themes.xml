<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MovieHelper" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/toss_blue</item>
        <item name="colorPrimaryVariant">@color/toss_blue_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/movie_accent</item>
        <item name="android:windowBackground">@color/backgroundColor</item>
    </style>

    <style name="Theme.MovieHelper" parent="Base.Theme.MovieHelper" />
    
    <!-- 캘린더 커스텀 테마 -->
    <style name="CustomCalendarTheme" parent="android:Theme.Material.Light">
        <item name="android:colorAccent">@color/toss_blue</item>
        <item name="android:textColorPrimary">@color/primaryTextColor</item>
        <item name="android:textColorSecondary">@color/secondaryTextColor</item>
    </style>
</resources>